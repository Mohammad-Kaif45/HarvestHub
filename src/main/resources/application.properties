# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/harvesthub_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Kaif@2004

# Hibernate Settings (Automatically creates tables for you)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf Settings (Disables cache so you don't have to restart server for HTML changes)
spring.thymeleaf.cache=false